- hosts: localhost
  connection: local
  tasks:
    - name: Установка пакетов
      become: true
      apt:
        pkg:
          - youtube-dl
          - openjdk-8-jre
          - libx11-dev
          - libxinerama-dev
          - libxft-dev
