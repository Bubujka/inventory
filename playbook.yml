- hosts: localhost
  connection: local
  tasks:
    - name: Установка пакетов
      become: true
      apt:
        pkg:
          - youtube-dl
          - openjdk-8-jre
    - import_tasks: tasks/dmenu.yml
